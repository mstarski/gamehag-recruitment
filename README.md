# Gamehag recruitment task

## Installing
Before running the dev server install necessary packages with `npm install`.

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`.
